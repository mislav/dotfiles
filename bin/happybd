#!/usr/bin/env bash
# Usage: happybd
#
# Outputs "Happy Birthday, Mislav!", if it is Mislav's birthday.
#
if [ `date '+%d%m'` == "0501" ]; then
  echo "Happy Birthday, Mislav!"
else
  echo "Nope, not today... But happy day, anyways!"
fi